{{- if and (.Values.gamma.enabled) (.Values.gammaUi.enabled) (not .Values.apim.dbLess) -}}
{{- if .Values.gammaUi.ingress.enabled -}}
{{- $serviceAPIName := include "gravitee.gammaUi.fullname" . -}}
{{- $serviceAPIPort := .Values.gammaUi.service.externalPort -}}
{{- $ingressPath   := .Values.gammaUi.ingress.path -}}
{{- $ingressPathType   := .Values.gammaUi.ingress.pathType -}}
{{- $apiVersion := include "common.capabilities.ingress.apiVersion" . -}}
apiVersion: {{ $apiVersion }}
kind: Ingress
metadata:
  name: {{ template "gravitee.gammaUi.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ template "gravitee.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Values.gammaUi.image.tag | default .Chart.AppVersion | quote }}
    app.kubernetes.io/component: "{{ .Values.gammaUi.name }}"
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    {{- if and .Values.common .Values.common.labels }}
    {{- range $key, $value := .Values.common.labels }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
  annotations:
    {{- if .Values.gammaUi.ingress.annotations }}
    {{- include "common.ingress.annotations.render" (dict "annotations" .Values.gammaUi.ingress.annotations "ingressClassName" .Values.gammaUi.ingress.ingressClassName "openshift" .Values.openshift "context" $) | nindent 4 }}
    {{- end }}
    {{- if and .Values.common .Values.common.annotations }}
    {{- range $key, $value := .Values.common.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
spec:
  {{- if and (eq (include "common.ingress.supportsIngressClassname" .) "true") (.Values.gammaUi.ingress.ingressClassName) (ne .Values.gammaUi.ingress.ingressClassName "none") }}
  ingressClassName: {{ .Values.gammaUi.ingress.ingressClassName | quote }}
  {{- end }}
  rules:
  {{- range $host := .Values.gammaUi.ingress.hosts }}
  - host: {{ $host | quote }}
    http:
      paths:
      - pathType: {{ $ingressPathType }}
        path: {{ $ingressPath }}
        backend:
          {{- if (eq $apiVersion "networking.k8s.io/v1") }}
          service:
            name: {{ $serviceAPIName }}
            port:
              number: {{ $serviceAPIPort }}
          {{ else }}
          serviceName: {{ $serviceAPIName }}
          servicePort: {{ $serviceAPIPort }}
          {{- end -}}
    {{- end -}}
  {{- if .Values.gammaUi.ingress.tls }}
  tls:
{{ toYaml .Values.gammaUi.ingress.tls | indent 4 }}
  {{- end -}}
{{- end -}}
{{- end -}}
