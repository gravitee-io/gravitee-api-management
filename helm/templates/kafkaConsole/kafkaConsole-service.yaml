{{- if .Values.kafkaConsole.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "gravitee.kafkaConsole.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ template "gravitee.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Values.kafkaConsole.image.tag | default .Chart.AppVersion | quote }}
    app.kubernetes.io/component: "{{ .Values.kafkaConsole.name }}"
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    {{- if and .Values.common .Values.common.labels }}
    {{- range $key, $value := .Values.common.labels }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
  annotations:
    {{- range $key, $value := .Values.kafkaConsole.service.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- if and .Values.common .Values.common.annotations }}
    {{- range $key, $value := .Values.common.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
spec:
  type: "{{ .Values.kafkaConsole.service.type }}"
  {{- if eq .Values.kafkaConsole.service.type "NodePort" "LoadBalancer" }}
  externalTrafficPolicy: {{ .Values.kafkaConsole.service.externalTrafficPolicy }}
  {{- end }}
  ports:
    - port: {{ .Values.kafkaConsole.service.externalPort }}
      targetPort: {{ .Values.kafkaConsole.service.internalPort }}
      {{- if eq .Values.kafkaConsole.service.type "NodePort" }}
      {{- if .Values.kafkaConsole.service.nodePort }}
      nodePort: {{ .Values.kafkaConsole.service.nodePort }}
      {{- end }}
      {{- end }}
      protocol: TCP
      {{- if (include "common.service.supportsAppProtocol" .) }}
      {{ if .Values.kafkaConsole.service.appProtocol }}
      appProtocol: {{ .Values.kafkaConsole.service.appProtocol }}
      {{ else }}
      appProtocol: http
      {{ end }}
      {{- end }}
      name: {{ .Values.kafkaConsole.name | trunc 63 | trimSuffix "-" }}
  selector:
    app.kubernetes.io/name: {{ template "gravitee.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: "{{ .Values.kafkaConsole.name }}"
{{- end -}}
