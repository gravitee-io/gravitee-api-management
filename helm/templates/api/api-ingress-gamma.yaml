{{- if and (.Values.gamma.enabled) (.Values.api.enabled) (not .Values.apim.dbLess) -}}
{{- if .Values.api.ingress.gamma.enabled -}}
{{- $serviceAPIName := include "gravitee.api.fullname" . -}}
{{- $serviceAPIPort := .Values.api.service.externalPort -}}
{{- $ingressPath   := .Values.api.ingress.gamma.path -}}
{{- $ingressPathType   := .Values.api.ingress.gamma.pathType -}}
{{- $apiVersion := include "common.capabilities.ingress.apiVersion" . -}}
apiVersion: {{ $apiVersion }}
kind: Ingress
metadata:
  name: {{ template "gravitee.api.fullname" . }}-gamma
  labels:
    app.kubernetes.io/name: {{ template "gravitee.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Values.api.image.tag | default .Chart.AppVersion | quote }}
    app.kubernetes.io/component: "{{ .Values.api.name }}"
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    {{- if and .Values.common .Values.common.labels }}
    {{- range $key, $value := .Values.common.labels }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
  annotations:
    {{- if .Values.api.ingress.gamma.annotations }}
    {{- include "common.ingress.annotations.render" (dict "annotations" .Values.api.ingress.gamma.annotations "ingressClassName" .Values.api.ingress.gamma.ingressClassName "openshift" .Values.openshift "context" $) | nindent 4 }}
    {{- end }}
    {{- if and .Values.common .Values.common.annotations }}
    {{- range $key, $value := .Values.common.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
spec:
  {{- if and (eq (include "common.ingress.supportsIngressClassname" .) "true") (.Values.api.ingress.gamma.ingressClassName) (ne .Values.api.ingress.gamma.ingressClassName "none") }}
  ingressClassName: {{ .Values.api.ingress.gamma.ingressClassName | quote }}
  {{- end }}
  rules:
  {{- range $host := .Values.api.ingress.gamma.hosts }}
  - host: {{ $host | quote }}
    http:
      paths:
      - pathType: {{ $ingressPathType }}
        path: {{ $ingressPath }}
        backend:
          {{- if (eq $apiVersion "networking.k8s.io/v1") }}
          service:
            name: {{ $serviceAPIName }}
            port:
              number: {{ $serviceAPIPort }}
          {{ else }}
          serviceName: {{ $serviceAPIName }}
          servicePort: {{ $serviceAPIPort }}
          {{- end -}}
  {{- end -}}
  {{- if .Values.api.ingress.gamma.tls }}
  tls:
{{ toYaml .Values.api.ingress.gamma.tls | indent 4 }}
  {{- end -}}
{{- end -}}
{{- end -}}
