suite: Test UI HTTPRoute
templates:
  - "ui/ui-httproute.yaml"
tests:
  - it: Does not render by default
    asserts:
      - hasDocuments:
          count: 0

  - it: Renders an HTTPRoute with explicit httpRoute values
    set:
      ui:
        ingress:
          enabled: false
        httpRoute:
          enabled: true
          gatewayName: traefik
          hostnames:
            - console.example.com
          pathMatchType: PathPrefix
          path: /console
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: HTTPRoute
      - isAPIVersion:
          of: gateway.networking.k8s.io/v1
      - equal:
          path: spec.parentRefs[0].name
          value: traefik
      - equal:
          path: spec.hostnames[0]
          value: console.example.com
      - equal:
          path: spec.rules[0].matches[0].path.type
          value: PathPrefix
      - equal:
          path: spec.rules[0].matches[0].path.value
          value: /console
      - equal:
          path: spec.rules[0].backendRefs[0].name
          value: RELEASE-NAME-apim-ui
      - equal:
          path: spec.rules[0].backendRefs[0].port
          value: 8002

  - it: Ignores ui.ingress values when both ingress and httpRoute are enabled
    set:
      ui:
        ingress:
          enabled: true
          hosts:
            - ingress.example.com
          pathType: Prefix
          path: /ingress-console
        httpRoute:
          enabled: true
          gatewayName: traefik
          hostnames:
            - console.example.com
          pathMatchType: PathPrefix
          path: /console
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: spec.hostnames[0]
          value: console.example.com
      - equal:
          path: spec.rules[0].matches[0].path.type
          value: PathPrefix
      - equal:
          path: spec.rules[0].matches[0].path.value
          value: /console

  - it: Fails when ui.httpRoute.path is missing
    set:
      ui:
        ingress:
          enabled: false
        httpRoute:
          enabled: true
          gatewayName: traefik
          hostnames:
            - console.example.com
          pathMatchType: PathPrefix
    asserts:
      - failedTemplate:
          errorMessage: "ui.httpRoute.path is required when ui.httpRoute.enabled=true"

  - it: Fails when ui.httpRoute.pathMatchType is missing
    set:
      ui:
        ingress:
          enabled: false
        httpRoute:
          enabled: true
          gatewayName: traefik
          hostnames:
            - console.example.com
          path: /console
    asserts:
      - failedTemplate:
          errorMessage: "ui.httpRoute.pathMatchType is required when ui.httpRoute.enabled=true"

  - it: Fails when ui.httpRoute.hostnames is empty
    set:
      ui:
        ingress:
          enabled: false
        httpRoute:
          enabled: true
          gatewayName: traefik
          pathMatchType: PathPrefix
          path: /console
    asserts:
      - failedTemplate:
          errorMessage: "ui.httpRoute.hostnames must contain at least one hostname when ui.httpRoute.enabled=true"

  - it: Renders custom parentRef path and middleware filters
    set:
      ui:
        ingress:
          enabled: false
        httpRoute:
          enabled: true
          gatewayName: public-gateway
          gatewayNamespace: gateways
          sectionName: websecure
          hostnames:
            - console.example.com
          pathMatchType: PathPrefix
          path: /console
          filters:
            - type: RequestHeaderModifier
              requestHeaderModifier:
                add:
                  - name: X-Forwarded-App
                    value: console
            - type: ExtensionRef
              extensionRef:
                group: traefik.io
                kind: Middleware
                name: secure-headers
    asserts:
      - equal:
          path: spec.parentRefs[0].namespace
          value: gateways
      - equal:
          path: spec.parentRefs[0].sectionName
          value: websecure
      - equal:
          path: spec.hostnames[0]
          value: console.example.com
      - equal:
          path: spec.rules[0].matches[0].path.type
          value: PathPrefix
      - equal:
          path: spec.rules[0].matches[0].path.value
          value: /console
      - equal:
          path: spec.rules[0].filters[0].type
          value: RequestHeaderModifier
      - equal:
          path: spec.rules[0].filters[0].requestHeaderModifier.add[0].name
          value: X-Forwarded-App

  - it: Renders generic URLRewrite and ExtensionRef filters as provided
    set:
      ui:
        ingress:
          enabled: false
        httpRoute:
          enabled: true
          gatewayName: traefik
          hostnames:
            - console.example.com
          pathMatchType: PathPrefix
          path: /console
          filters:
            - type: URLRewrite
              urlRewrite:
                path:
                  type: ReplacePrefixMatch
                  replacePrefixMatch: /
            - type: ExtensionRef
              extensionRef:
                group: traefik.io
                kind: Middleware
                name: strip-console-prefix
    asserts:
      - equal:
          path: spec.rules[0].filters[0].type
          value: URLRewrite
      - equal:
          path: spec.rules[0].filters[0].urlRewrite.path.type
          value: ReplacePrefixMatch
      - equal:
          path: spec.rules[0].filters[1].extensionRef.name
          value: strip-console-prefix

  - it: Does not render in dbLess mode
    set:
      apim:
        dbLess: true
      ui:
        httpRoute:
          enabled: true
          gatewayName: traefik
    asserts:
      - hasDocuments:
          count: 0