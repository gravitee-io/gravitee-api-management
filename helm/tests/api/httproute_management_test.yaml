suite: Test API Management HTTPRoute
templates:
  - "api/api-httproute-management.yaml"
tests:
  - it: Does not render by default
    asserts:
      - hasDocuments:
          count: 0

  - it: Renders an HTTPRoute with explicit httpRoute values
    set:
      api:
        ingress:
          management:
            enabled: false
        httpRoute:
          management:
            enabled: true
            gatewayName: traefik
            hostnames:
              - api.example.com
            pathMatchType: PathPrefix
            path: /management
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: HTTPRoute
      - isAPIVersion:
          of: gateway.networking.k8s.io/v1
      - equal:
          path: spec.parentRefs[0].name
          value: traefik
      - equal:
          path: spec.hostnames[0]
          value: api.example.com
      - equal:
          path: spec.rules[0].matches[0].path.type
          value: PathPrefix
      - equal:
          path: spec.rules[0].matches[0].path.value
          value: /management
      - equal:
          path: spec.rules[0].backendRefs[0].name
          value: RELEASE-NAME-apim-api
      - equal:
          path: spec.rules[0].backendRefs[0].port
          value: 83

  - it: Ignores api.ingress.management values when both ingress and httpRoute are enabled
    set:
      api:
        ingress:
          management:
            enabled: true
            hosts:
              - ingress.example.com
            pathType: Prefix
            path: /management
        httpRoute:
          management:
            enabled: true
            gatewayName: traefik
            hostnames:
              - route.example.com
            pathMatchType: RegularExpression
            path: /mgmt(/.*)?
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: spec.hostnames[0]
          value: route.example.com
      - equal:
          path: spec.rules[0].matches[0].path.type
          value: RegularExpression
      - equal:
          path: spec.rules[0].matches[0].path.value
          value: /mgmt(/.*)?

  - it: Fails when api.httpRoute.management.path is missing
    set:
      api:
        httpRoute:
          management:
            enabled: true
            gatewayName: traefik
            hostnames:
              - api.example.com
            pathMatchType: PathPrefix
    asserts:
      - failedTemplate:
          errorMessage: "api.httpRoute.management.path is required when api.httpRoute.management.enabled=true"

  - it: Fails when api.httpRoute.management.pathMatchType is missing
    set:
      api:
        httpRoute:
          management:
            enabled: true
            gatewayName: traefik
            hostnames:
              - api.example.com
            path: /management
    asserts:
      - failedTemplate:
          errorMessage: "api.httpRoute.management.pathMatchType is required when api.httpRoute.management.enabled=true"

  - it: Fails when api.httpRoute.management.hostnames is empty
    set:
      api:
        httpRoute:
          management:
            enabled: true
            gatewayName: traefik
            pathMatchType: PathPrefix
            path: /management
    asserts:
      - failedTemplate:
          errorMessage: "api.httpRoute.management.hostnames must contain at least one hostname when api.httpRoute.management.enabled=true"

  - it: Renders custom parentRef, path and filters
    set:
      api:
        ingress:
          management:
            enabled: false
        httpRoute:
          management:
            enabled: true
            gatewayName: public-gateway
            gatewayNamespace: gateways
            sectionName: websecure
            hostnames:
              - api.example.com
            pathMatchType: PathPrefix
            path: /management
            filters:
              - type: ExtensionRef
                extensionRef:
                  group: traefik.io
                  kind: Middleware
                  name: secure-headers
    asserts:
      - equal:
          path: spec.parentRefs[0].namespace
          value: gateways
      - equal:
          path: spec.parentRefs[0].sectionName
          value: websecure
      - equal:
          path: spec.hostnames[0]
          value: api.example.com
      - equal:
          path: spec.rules[0].matches[0].path.type
          value: PathPrefix
      - equal:
          path: spec.rules[0].matches[0].path.value
          value: /management
      - equal:
          path: spec.rules[0].filters[0].extensionRef.name
          value: secure-headers

  - it: Does not render in dbLess mode
    set:
      apim:
        dbLess: true
      api:
        httpRoute:
          management:
            enabled: true
            gatewayName: traefik
    asserts:
      - hasDocuments:
          count: 0

  - it: Renders from explicit httpRoute values when ingress is disabled
    set:
      api:
        ingress:
          management:
            enabled: false
        httpRoute:
          management:
            enabled: true
            gatewayName: traefik
            hostnames:
              - api.example.com
            pathMatchType: PathPrefix
            path: /management
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: spec.hostnames[0]
          value: api.example.com
      - equal:
          path: spec.rules[0].matches[0].path.type
          value: PathPrefix
      - equal:
          path: spec.rules[0].matches[0].path.value
          value: /management

  - it: Does not render when api is disabled
    set:
      api:
        enabled: false
        httpRoute:
          management:
            enabled: true
            gatewayName: traefik
    asserts:
      - hasDocuments:
          count: 0
