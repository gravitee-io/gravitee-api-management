== Gravitee API Management - Console

== Description

This repo contains the source code of APIM Console.

APIM Console is a client-side only Angular application and can be deployed on any HTTP server, such as Apache or Nginx.

For more information about installation and usage, see https://documentation.gravitee.io/apim/getting-started/install-guides[Gravitee.io Documentation Website].

== Architecture

This project is part of an **Nx workspace** at the repository root.

- **Intelligent caching**: Nx caches build and test results
- **Affected commands**: Only test/build what changed
- **Dependency graph**: Visualize project dependencies with `yarn graph`
- **Unified configuration**: All dependencies managed at the root

Learn more about Nx at https://nx.dev[nx.dev]

== Contributing

=== Install

Prerequisites:

- Install https://github.com/nvm-sh/nvm[nvm]
- Use with `nvm use` or install with `nvm install` the version of Node.js declared in `.nvmrc` (at repository root)

- Then install dependencies from the **repository root**:
[source,bash]
----
# Go to repository root
yarn install
----

=== Getting started

All commands must be run from the **repository root** (not from this directory).

Here are the useful commands available when developing APIM Console:

Development::
 - `yarn console:serve` - Start the app in dev mode (with hot reload) and proxy backend calls to `http://localhost:8083`
 - `yarn console:serve:apim-master` - Start the app and proxy backend calls to `https://apim-master-api.team-apim.gravitee.dev`
 - `yarn console:serve:apim-gko` - Start the app and proxy backend calls to `https://apim-master-gko-api.team-apim.gravitee.dev`

Testing & Quality::
 - `yarn console:test` or `nx test console` - Run unit tests with Jest
 - `yarn console:lint` or `nx lint console` - Run ESLint
 - `yarn console:storybook` - Start Storybook on port 9008

Build::
 - `yarn console:build` or `nx build console` - Build the app in development mode
 - `yarn console:build:prod` or `nx build:prod console` - Build the app in production mode

=== About gravitee-markdown and gravitee-dashboard libraries

This project uses the `@gravitee/gravitee-markdown` and `@gravitee/gravitee-dashboard` libraries.

These libraries are now located in `gravitee-apim-webui-libs/` (moved from `portal-webui-next/projects/`).

Building libraries::
The libraries are automatically built by Nx when needed.

