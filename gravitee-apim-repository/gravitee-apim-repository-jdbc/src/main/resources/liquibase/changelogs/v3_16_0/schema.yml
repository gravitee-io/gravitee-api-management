databaseChangeLog:
  - changeSet:
      id: 3.16.0
      author: GraviteeSource Team
      changes:
        - addColumn:
            tableName: ${gravitee_prefix}applications
            columns:
              - column:
                  name: api_key_mode
                  type: nvarchar(256)
                  constraints:
                    nullable: true
